# Language setting
language: python

#runtime:
 # nodePool: deepika-test-nodePool
 
# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - apt-get install cowsay
    - apt-get install python-pip

   
