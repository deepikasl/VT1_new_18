language: python

runtime:
  nodePool: default-restricted
  
python:
  - 2.7

build:
  ci:
    - docker build -t=deepikasl/sample_node_pvt .
    - echo "here-restricted-node-pool"
     
  post_ci:
    - docker push deepikasl/sample_node_pvt
  
integrations:
  generic: 
    - integrationName: deepika-docker
      type: dockerRegistryLogin
