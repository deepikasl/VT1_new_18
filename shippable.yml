# Language setting
language: python

#runtime:
 # nodePool: deepika-test-nodePool
 
# Version number
python:
  - ppy

build:
  ci:
    #- sleep 360
    - python --version
    - apt-get install cowsay
    - apt-get install python-pip

