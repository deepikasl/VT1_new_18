# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - echo "you are here"
   # - shipctl put_resource_state "test_params" TRIGGER_REASON BaseImgChanged

