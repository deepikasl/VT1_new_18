# Language setting
language: python

#runtime:
 # nodePool: deepika-test-nodePool
 
 
# Version number
python:
  - 2.7

build:
  ci:
    #- sleep 360
    - python --version
    - apt-get install cowsay
    - apt-get install python-pip
    - echo "check this update issue"
